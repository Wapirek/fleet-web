import { DisplayColsNamePipe } from 'src/app/shared/pipes/display-cols-name/display-cols-name.pipe';

describe('DisplayColsNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayColsNamePipe();
    expect(pipe).toBeTruthy();
  });
});
